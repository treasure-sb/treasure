import Exit from "./components/Exit";

export default function Page() {
  return (
    <main className="max-w-[var(--container-width)] mx-auto">
      <Exit />
      <span>Create Event </span>
    </main>
  );
}
